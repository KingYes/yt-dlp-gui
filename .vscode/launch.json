{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run yt-dlp GUI",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "python": "${workspaceFolder}/venv/bin/python3",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
