{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run yt-dlp GUI",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "python": "${command:python.interpreterPath}",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
