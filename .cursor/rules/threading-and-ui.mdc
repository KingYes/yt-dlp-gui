---
description: Threading model and main-thread safety conventions
globs: "**/*.py"
alwaysApply: false
---

# Threading & UI Safety

All I/O, downloads, and network calls **must** run in background daemon threads — never block the main (UI) thread.

## Main-Thread Dispatcher

Use `Signal(object)` + `_schedule_on_main(func)` to marshal callbacks from worker threads to the main thread. The Signal is connected to `_exec_on_main` which executes the callable on the Qt main thread.

## Rules

- **Daemon threads only** — set `daemon=True` on all background `threading.Thread` instances.
- **`DownloadManager` must never import or reference UI widgets.** Communicate results via callbacks (`progress_callback`, `item_done_callback`, `done_callback`).
- **Cancellation** — use `threading.Event` (`_cancel_event`). Never kill threads.
- **Single background thread per download session** — no thread pools. Sequential batch downloads use one thread iterating over URLs.
- **Progress throttling** — `_on_progress` skips `downloading` events within 50 ms of the last one to reduce Signal flooding.
- **`QTimer.singleShot()`** for deferred main-thread work (startup checks, queue processing).
- **Shutdown guard** — `_exec_on_main` checks `_shutting_down` flag to discard callbacks arriving after window destruction.
